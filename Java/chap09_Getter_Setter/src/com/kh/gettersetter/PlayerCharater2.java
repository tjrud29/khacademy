package com.kh.gettersetter;

public class PlayerCharater2 {
	public class PlayerCharacter2 {
		/*
		@Getter(return name)  private String name;   		//캐릭터이름
		@Getter @Setter private   int  health; 		//체력
		@Getter @Setter private   int  attackPower; //공격파워
		public void setHealth(int health) {
			// 만약에 체력이 0이라면 죽이고 체력이 1이상 이면 살리자
			if(health >= 1 ) { // 체력이 1 이상일 경우
				this.health = health;
			} else { // 체력이 0 이하라면 체력을 표시할 가치도 없다.
				this.health = health;
			}
			
		}
		
		//진짜 만약의 경우의 수가 없는지?
		//public void isValidName(){
		
			if(name이 공백이라면?) {
				
			}
		//캐릭터 정보 출력
		public void displayInfo() {
			System.out.println("캐릭터 정보 : " + name);
			System.out.println("체력 정보 : " + health);
			System.out.println("공격 파워 : " + attackPower);
		}
		
		//캐릭터 공격하는 메서드
		public void attack(PlayerCharacter2 target) {
			System.out.println("["+name+"]는 공격해서 [" + target.getName()+"] 한테 " + attackPower+" 데미지를 줬습니다!");
			System.out.println("공격했습니다.");
		}
		
		//다른 캐릭터로부터 공격 받았을 때 메서드
		public void takeDamage(int damage) {
			System.out.println("공격 받았습니다.");
		}
		*/
	}
}
